-- Quality dataset: rule occurrences and the published quality report.
CREATE SCHEMA IF NOT EXISTS `PROJECT.quality`
OPTIONS (
    location    = 'US',
    description = 'Quality: rule occurrences and quality report'
);
